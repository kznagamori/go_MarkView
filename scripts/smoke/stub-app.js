// wailsjs/go/main/App.js の代役（BR-054）。
//
// api.js は `import * as App` で取り込むため、名前解決は参照した時点まで
// 遅れる。描画（lazy.js）は Go を呼ばないので、空のモジュールで足りる。
//
// **本物を配らない理由。** frontend/wailsjs/ は wails build が毎回生成する
// もので .gitignore の対象である（AR-050）。スモークテストがそれに依存すると
// 「ビルド済みの作業ツリーでしか動かない検証」になり、資産だけを更新した
// 直後に走らせる BR-043 の手順で成り立たない。
export {};
