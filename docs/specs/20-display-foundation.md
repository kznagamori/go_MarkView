# 20. 表示仕様: デザイン基礎

> 索引: [README](README.md) | 表示仕様: **20** / [21](21-display-components.md) / [22](22-display-states.md)

本文書から始まる表示仕様書（`DSP-nnn`）は、要求仕様書が定めた画面要求（`UI-nnn`）と描画要求（`MD-nnn`）に対して、**どう見えるか**を具体値で定める。構造とクラス名は実装仕様書（[12](12-impl-frontend.md)）で定めており、本文書はそこに色・寸法・書体を与える。

## 20.1 本文書群の位置づけ

### DSP-001: 値の正 **MUST**

- 本文書に記す色・寸法は**策定時点の代表値**である。本文の描画（`.markdown-body` 配下）については、同梱する `github-markdown-css` の定義を正とし、値が食い違う場合は同梱物の定義を優先する（MD-001）。
- アプリ自身の UI（ツールバー・ペイン・ステータス・検索バー・ダイアログ・**拡大画面・右クリックメニュー・本文中に足したボタン・編集モードの枠とラベルとセルの編集欄**）については、**本文書の値を正**とする。本文と地続きに見えるよう、同じトークン体系から色を採る。
- 資産の更新（BR-043）で本文の配色が変わった場合、アプリ UI 側のトークンも追随させ、両者の色がずれた状態を放置しない。

### DSP-002: 実装との対応 **MUST**

本文書の値は `frontend/css/tokens.css` に CSS カスタムプロパティとして実装する（IMP-200）。個々のコンポーネントの CSS はトークンを参照し、色コードを直接書かない。

## 20.2 カラートークン（DSP-010 系）

### DSP-010: 基本トークン **MUST**

UI-105 を実装する。Light / Dark の 2 系統を定義する。

| トークン | 用途 | Light | Dark |
| --- | --- | --- | --- |
| `--canvas-default` | 本文・ダイアログの背景 | `#ffffff` | `#0d1117` |
| `--canvas-subtle` | ペイン・ツールバー・コードブロックの背景 | `#f6f8fa` | `#151b23` |
| `--canvas-inset` | 入力欄・凹んで見せる領域 | `#f6f8fa` | `#010409` |
| `--canvas-overlay` | ダイアログ・ポップオーバーの背景 | `#ffffff` | `#151b23` |
| `--fg-default` | 本文の文字 | `#1f2328` | `#e6edf3` |
| `--fg-muted` | 補助テキスト、ステータスの副次情報 | `#59636e` | `#9198a1` |
| `--fg-subtle` | さらに淡いテキスト、プレースホルダ | `#818b98` | `#6e7681` |
| `--fg-on-emphasis` | 濃い背景の上に載る文字 | `#ffffff` | `#ffffff` |
| `--border-default` | ペイン境界・表の罫線・ボタン枠 | `#d1d9e0` | `#3d444d` |
| `--border-muted` | 弱い区切り線 | `#d8dee4` | `#2f353d` |
| `--accent-fg` | リンク、選択中の強調 | `#0969da` | `#4493f8` |
| `--accent-emphasis` | 選択項目の背景（濃） | `#0969da` | `#1f6feb` |
| `--accent-subtle` | 選択項目の背景（淡） | `#ddf4ff` | `#121d2f` |
| `--success-fg` | Tip、成功表示 | `#1a7f37` | `#3fb950` |
| `--attention-fg` | Warning | `#9a6700` | `#d29922` |
| `--danger-fg` | Caution、エラー | `#d1242f` | `#f85149` |
| `--done-fg` | Important | `#8250df` | `#ab7df8` |
| `--neutral-muted` | ホバー背景、コードのインライン背景 | `rgba(129,139,152,0.12)` | `rgba(101,108,118,0.2)` |
| `--neutral-muted-strong` | ボタン押下中の背景（`--neutral-muted` の 1.5 倍） | `rgba(129,139,152,0.18)` | `rgba(101,108,118,0.3)` |
| `--attention-subtle` | 警告の一時メッセージの背景、本文のハイライト（`<mark>`） | `#fff8c5` | `rgba(187,128,9,0.15)` |
| `--danger-subtle` | エラーの一時メッセージの背景 | `#ffebe9` | `rgba(248,81,73,0.15)` |
| `--shadow-overlay` | 浮かぶ部品（ダイアログ・検索バー・右クリックメニュー）の影 | `0 8px 24px rgba(31,35,40,0.2)` | `0 8px 24px rgba(1,4,9,0.6)` |
| `--overlay-backdrop` | ダイアログ背後の暗幕 | `rgba(0,0,0,0.4)` | `rgba(0,0,0,0.4)` |
| `--search-hit` | 検索ヒット（通常） | `#fff8c5` | `#3b2300` |
| `--search-hit-current` | 検索ヒット（現在位置） | `#ffd33d` | `#9e6a03` |

### DSP-011: テーマの適用機構 **MUST**

```css
:root { /* Light を既定として定義 */ }
[data-theme="dark"] { /* Dark で上書き */ }
```

- 切り替えは `#app` の `data-theme` 属性のみで行う（IMP-243）。要素の再生成や本文の再変換を伴わない（UI-105）。
- `prefers-color-scheme` に直接反応する CSS を書かない。OS 設定の反映は起動時に Go 側が解決した値で行う（FR-071, IMP-303）。両方に反応させると、設定値と OS 設定が食い違ったときに表示が揺れる。
- **`color-scheme` をテーマに合わせて宣言する。** タスクリストのチェックボックス（MD-022）のように OS が描く部品は、色を CSS のトークンで指定できない。この宣言がないと Dark でも明るい配色のまま浮く。
- トランジションを色に対して掛けない。テーマ切り替えは即時に完了させる（UI-105 の「ちらつきを発生させない」）。

### DSP-012: 本文の配色 **MUST**

`.markdown-body` 配下は `github-markdown-css` の定義に従う（DSP-001）。同梱物が定義する CSS 変数と、本文書のトークンを以下のとおり対応させ、片方だけが変わることを防ぐ。

| github-markdown-css の変数 | 本文書のトークン |
| --- | --- |
| `--color-canvas-default` | `--canvas-default` |
| `--color-canvas-subtle` | `--canvas-subtle` |
| `--color-fg-default` | `--fg-default` |
| `--color-fg-muted` | `--fg-muted` |
| `--color-border-default` | `--border-default` |
| `--color-accent-fg` | `--accent-fg` |

`markdown.css` はこの 6 つを `.markdown-body` の上で定義し、本文の規則からはトークンではなくこちらを参照する。対応表を 1 か所に閉じ込めるためである。表に無い色（`--border-muted` など）はトークンを直接参照してよい。

> [!NOTE]
> **同梱物側の変数名は改称されている。** `github-markdown-css` は v5.9.0 の時点で `--fgColor-default` / `--bgColor-default` / `--fgColor-muted` / `--borderColor-default` / `--fgColor-accent` という名前へ移行しており、上の表が挙げる `--color-*` は既に使っていない。現在の `markdown.css` は同梱物のファイルを読み込まず書き起こしているため実害はないが、将来ファイルをそのまま読み込む形へ変える場合は、この表を新しい名前へ揃える必要がある。

### DSP-013: シンタックスハイライト配色 **MUST**

MD-030 / IMP-114 を実装する。chroma はクラス名のみを出力するため、配色は CSS で与える。

| chroma クラス | 意味 | Light | Dark |
| --- | --- | --- | --- |
| `.k`, `.kd`, `.kn` | キーワード | `#cf222e` | `#ff7b72` |
| `.s`, `.s1`, `.s2` | 文字列 | `#0a3069` | `#a5d6ff` |
| `.mi`, `.mf` | 数値 | `#0550ae` | `#79c0ff` |
| `.c`, `.c1`, `.cm` | コメント | `#59636e` | `#9198a1` |
| `.nf`, `.nx` | 関数名・識別子 | `#8250df` | `#d2a8ff` |
| `.nt` | タグ名 | `#116329` | `#7ee787` |
| `.na` | 属性名 | `#0550ae` | `#79c0ff` |
| `.o`, `.p` | 演算子・区切り | `#1f2328` | `#e6edf3` |
| `.gd` | diff 削除行 | 文字 `#82071e` / 背景 `#ffebe9` | 文字 `#ffdcd7` / 背景 `#67060c` |
| `.gi` | diff 追加行 | 文字 `#116329` / 背景 `#dafbe1` | 文字 `#aff5b4` / 背景 `#033a16` |
| `.err` | 字句エラー | `--fg-default`（強調しない） | 同左 |

`.err` を目立たせないのは、未対応の言語や部分的なパース失敗で本文が赤く染まるのを避けるためである。

### DSP-014: アイコンの表示 **MUST**

UI-022 / IMP-203 を実装する。

| 項目 | 値 |
| --- | --- |
| 描画サイズ | 16 × 16 px |
| 色（通常） | `--fg-muted` |
| 色（ホバー・フォーカス） | `--fg-default` |
| 色（トグル ON） | `--accent-fg` |
| 色（無効） | `--fg-subtle` |
| 塗り | `fill: currentColor` |

### DSP-015: 重なり順 **MUST**

`z-index` は以下の値のみを使う。中間値を場当たりに追加しない。

| 値 | 対象 |
| --- | --- |
| `0` | 本文 |
| `10` | コピーボタン、**図と画像のボタン（DSP-124）、並べ替えのボタン（DSP-125）、セルの編集欄（DSP-127）**、リサイザ |
| `20` | 検索バー、**編集モードの枠とラベル（DSP-126）** |
| `30` | ステータスの一時メッセージ |
| `40` | 状態画面 |
| `50` | ダイアログのオーバーレイ（情報 DSP-170 / エディタ選択 DSP-172）、**拡大画面（DSP-173）** |
| `60` | **右クリックメニュー（DSP-140）** |
| `70` | ドロップオーバーレイ |
| `80` | ツールチップ |

ドロップオーバーレイを高い位置に置くのは、ダイアログ表示中にファイルをドラッグした場合でもドロップ可能であることを示すためである（UI-070 の「ウィンドウ全体」）。ツールチップはさらに上に置く。どの文脈でもボタンの説明が隠れずに読めるようにするためで、ドラッグ中はツールチップ自体が表示されないため競合しない。

- **右クリックメニューはダイアログより上に置く。** 情報ダイアログのライセンス欄でも開くためである（FR-063）。**ドロップオーバーレイよりは下に置く**——メニューを開いたままファイルをドラッグしてきた場合も、ドロップできることを示す（UI-090）。
- **拡大画面はダイアログと同じ層に置く。** 両者は同時に開かない（拡大画面は本文のボタンから開き、ダイアログの表示中は本文を操作できない）。**ダイアログの中身を Go から受け取るのを待つ間に拡大画面を開いた場合も、ダイアログを出さない**（IMP-251, IMP-252）。同じ層で DOM では `#expand-view` が後ろにあるため、出すと拡大画面の下に隠れる。
- **編集モードの枠とラベルは検索バーと同じ層に置く。** ラベルと検索バーは置き場所が右下と右上で重ならない（DSP-126, DSP-160）。枠は `pointer-events: none` で描画だけを行い、検索バーの下に来ても操作を妨げない。

> [!NOTE]
> **4.41.0 でドロップオーバーレイを 60 → 70、ツールチップを 70 → 80 へ動かした。** 右クリックメニューの層を、ダイアログとドロップオーバーレイの間に入れるためである。**「中間値を場当たりに追加しない」ため、65 のような値を足さずに表を詰め直した。** CSS の値も同じ変更で直す。`tokens.css` の `--z-dropzone` を 70、`--z-tooltip` を 80 にし、右クリックメニューの `--z-contextmenu`（60）を足す（拡大画面はダイアログと同じ `--z-dialog` を使う）。**`tokens.css` と `components.css` の「8 段階」という注記も 9 段階へ直す。**

### DSP-016: フォーカス表示 **MUST**

- キーボード操作時のフォーカスリングを消さない（IMP-295）。
- 表現は `outline: 2px solid var(--accent-fg); outline-offset: -1px;` とする。
- `:focus-visible` を用い、マウスクリックによるフォーカスではリングを出さない。
- **例外は次のとおりとし、これ以外に作らない。** ツリーはリングを `li` ではなく行（`.tree-row`）に出す（DSP-330）。セルの編集欄は枠そのものがフォーカスを示すため、リングを重ねない（DSP-127）。

### DSP-017: アプリケーションアイコン **MUST**

UI-025 を実装する。ツールバー内の単色アイコン（DSP-014）とは扱いが異なり、**テーマに追従せず、色を変えない固有の画像**である。

| 用途 | ファイル | 表示サイズ |
| --- | --- | --- |
| Windows 実行ファイル | `assets/icon.ico` | OS が用途に応じて内蔵サイズから選択（16 / 32 / 48 / 256 px を含めること） |
| ウィンドウ・タスクバー | Windows: `assets/icon.ico` / Linux: `assets/icon.png` | OS が選択 |
| 情報ダイアログ | `/appicon.png`（IMP-160 が配信） | 48 × 48 論理ピクセル |

- 情報ダイアログでは角丸を付けず、縦横比を維持して表示する。背景色を敷かない。
- **Light テーマと Dark テーマの双方で視認できる絵柄であること。** 白背景・黒背景のどちらに置いても輪郭が消えないよう、単色の塗り潰しや極端に明るい/暗い配色を避ける。テーマごとに画像を切り替える仕組みは持たない。
- 高 DPI 環境で拡大されるため、`icon.png` は 256 × 256 px 以上の解像度を持つこと（DSP-381）。
- 文書未表示時の案内画面には使用しない（DSP-181）。そこでは行動を示す `icon-open` を用いる。

## 20.3 タイポグラフィ（DSP-020 系）

### DSP-020: 書体と字送り **MUST**

MD-010 / MD-011 を実装する。

| 用途 | フォントスタック | サイズ | 行の高さ |
| --- | --- | --- | --- |
| 本文 | `-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", Helvetica, Arial, "Yu Gothic UI", Meiryo, "Noto Sans CJK JP", sans-serif` | 16 px | 1.6 |
| コード | `ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", "Noto Sans Mono", monospace` | 13.6 px（本文の 85 %） | 1.45 |
| アプリ UI（ツールバー・ペイン） | 本文と同じスタック | 13 px | 1.5 |
| ステータス領域 | 本文と同じスタック | 12 px | 1.4 |
| ペイン見出し（`Files` / `Outline`） | 本文と同じスタック | 11 px（大文字・字間 0.04em） | 1.4 |

### DSP-021: 表示倍率の適用範囲 **MUST**

FR-081 / IMP-242 を実装する。

```css
.markdown-body { font-size: calc(16px * var(--zoom, 100) / 100); }
```

- `--zoom` は `#markdown` に設定する。ツールバー・ペイン・ステータスには影響させない（FR-081）。
- 本文内の余白・コードブロック・表の寸法は `em` で定義し、倍率に追随させる。
- **画像・Mermaid 図・PlantUML 図は倍率に追随しない。** 固有の大きさ（px）を持ち、本文幅（`max-width: 100%`）で頭打ちになるだけである。**本文幅そのものが 980 論理ピクセルの固定値**（UI-050）であるため、倍率を変えても上限は動かない。
- **タスクリストのチェックボックスも追随しない**（MD-022）。エンジンが描く部品であり、寸法を CSS で与えていない（NFR-061 の許容差）。
- **原寸表示（DSP-124）の「本来の大きさ」も倍率に追随しない**（FR-121）。本来の大きさは論理ピクセルであり、`--zoom` を掛けない。**拡大画面（DSP-173）の倍率は本文の倍率と独立している。**
- **本文中に足したボタン（コピー・図と画像・並べ替え）とセルの編集欄は、`--zoom` の影響を受けない寸法（px）で与える。** 押す対象の大きさが倍率で変わらないようにする（DSP-032 のツールバーのボタンと同じ考え方）。**セルの編集欄の文字の大きさだけは、セルの文字に合わせて倍率に追随させる**（DSP-127）。

> [!IMPORTANT]
> **4.35.0 より前は「画像・図は本文幅に従うため、結果として倍率に追随する」と書いていた。誤りである。**
> 実測（本物の `frontend/css/` を読み込み、`zoom.js` と同じく `#markdown` の `--zoom` を動かした）:
>
> | 倍率 | 本文の器 | 本文の文字 | 400px の画像 | 600px の図 | チェックボックス |
> | --- | --- | --- | --- | --- | --- |
> | 100 % | 980px | 16px | 400px | 600px | 13px |
> | 300 % | **980px** | **48px** | **400px** | **600px** | **13px** |
>
> **「本文幅に従う」ことは「倍率に追随する」ことにならない。** 本文幅が固定である以上、追随のしようがない。**倍率が変えるのは文字の大きさと、`em` で定義した余白だけである。**
>
> これは `FR-081`（「**本文の**表示倍率を拡大・縮小する」）に反しない。**確認は E2E-273 の確認内容 8 が行う。**

### DSP-022: 見出しの階層 **MUST**

MD-020 を実装する。値は本文サイズ（`1em` = 16 px）に対する相対値。

| 要素 | サイズ | 上マージン | 下マージン | 下線 |
| --- | --- | --- | --- | --- |
| `h1` | 2em | 24px | 16px | あり（`--border-muted`） |
| `h2` | 1.5em | 24px | 16px | あり（`--border-muted`） |
| `h3` | 1.25em | 24px | 16px | なし |
| `h4` | 1em | 24px | 16px | なし |
| `h5` | 0.875em | 24px | 16px | なし |
| `h6` | 0.85em（`--fg-muted`） | 24px | 16px | なし |

### DSP-023: 見出しのアンカーアイコン **SHOULD**

MD-020 / IMP-227 を実装する。フックは `a.heading-anchor`。

| 項目 | 値 |
| --- | --- |
| 位置 | 見出しの**左外側**。`h1`〜`h6` に `position: relative` を与え、`position: absolute` で `left: -1.2em` に置く |
| 大きさ | `.icon` と同じ 16 × 16（DSP-014）。倍率には追随しない |
| 色 | `--fg-muted` |
| 既定 | `opacity: 0` |
| 表示する条件 | 見出しの `:hover`、および `:focus-within`（キーボード操作でも出す。DSP-016） |
| 遷移 | `opacity` のみ 0.1 秒（DSP-050）。`prefers-reduced-motion` では即時（DSP-051） |

- **本文の折り返しに影響させない。** 絶対配置とし、見出しのテキストの開始位置を動かさない。
- 幅が足りない環境では枠外へはみ出すため、`#markdown` に `overflow-x` を新たに与えない（UI-050 の「横スクロールを発生させない」）。**はみ出した分は見えなくてよい。** アンカーは補助であり、見えないことで文書が読めなくなるものではない。
- 見出しにマウスを乗せていない間は、**空白も占有しない**（絶対配置のため）。

## 20.4 寸法（DSP-030 系）

### DSP-030: レイアウト寸法 **MUST**

UI-010 / UI-011 / UI-023 / UI-030 / UI-040 / UI-050 / UI-055 / UI-060 / UI-085 / UI-104 を実装する。単位はすべて論理ピクセル。

| 対象 | 値 | 根拠 |
| --- | --- | --- |
| ウィンドウ初期サイズ | 1280 × 860 | UI-011 |
| ウィンドウ最小サイズ | 640 × 480 | UI-011 |
| ツールバー高さ | 40 | UI-023 |
| ステータス領域高さ | 24 | UI-060 |
| ファイルツリーペイン幅（既定 / 最小 / 最大） | 260 / 160 / ウィンドウ幅の 40 % | UI-030 |
| アウトラインペイン幅（既定 / 最小 / 最大） | 240 / 160 / ウィンドウ幅の 40 % | UI-040 |
| リサイザの幅（見た目 / 当たり判定） | 1 / 8 | — |
| 本文の最大幅 | 980（**左右パディングを含む**。`box-sizing: border-box`。文字の並ぶ幅は最大 916） | UI-050 |
| 本文の左右パディング | 32（最小 16。`clamp(16px, 4vw, 32px)`） | UI-050 |
| 本文の上下パディング | 24 | — |
| 情報ダイアログ | 720 × 560 | UI-100 |
| エディタ選択ダイアログ | 幅 420（高さは内容に従う） | UI-103 |
| 拡大画面 | ウィンドウの幅 × （ウィンドウの高さ − ステータス領域の 24） | UI-104 |
| 拡大画面の操作バーの高さ | 40（ツールバーと同じ） | UI-104 |
| 右クリックメニューの幅（最小） | 180 | UI-085 |
| 編集モードの枠の太さ | 2 | UI-055 |

### DSP-031: 間隔の基準 **SHOULD**

余白は 4 px を基準単位とし、`4 / 8 / 12 / 16 / 24 / 32` から選ぶ。中間値を使わない。

- **例外は次に限る。** 表のセルのパディング（DSP-122 の `6px 13px`。`github-markdown-css` の値。DSP-001）と、そこから導く値（DSP-125 のボタンの位置 13px と見出しセルの余白 37px、DSP-127 の編集欄の `4px 11px`）、枠の太さや位置の微調整（DSP-140 のメニューを点から 2px 離す、など）。**導いた値は、導き方を表に書く。**

### DSP-032: ツールバー内の寸法 **MUST**

| 対象 | 値 |
| --- | --- |
| ボタンのクリック領域 | 28 × 28 |
| ボタン間の間隔 | 4 |
| ツールバー左右のパディング | 8 |
| ファイル・再読み込み・テーマの群と、アウトライン・ファイルツリー・エディタ・**編集モード**の群の間隔 | 12 |

「?」ボタンは右端に寄せ、他のボタンとの間に伸縮する余白（`flex: 1`）を置く（UI-020）。

## 20.5 装飾（DSP-040 系）

### DSP-040: 角丸・境界・影 **MUST**

| 対象 | 値 |
| --- | --- |
| ボタン・入力欄の角丸 | 6px |
| コードブロック・Alerts の角丸 | 6px |
| ダイアログの角丸 | 12px |
| ペイン境界 | `1px solid var(--border-default)` |
| 浮かぶ部品の影（ダイアログ・検索バー・右クリックメニュー） | `var(--shadow-overlay)` |
| ボタンのホバー背景 | `var(--neutral-muted)` |

影は本文の上に浮かぶ部品（ダイアログ DSP-170・検索バー DSP-160・右クリックメニュー DSP-140）にのみ用いる。ペイン・ツールバー・ドロップオーバーレイ・拡大画面（画面いっぱいの面であり浮かばない）に影を落とさない。

### DSP-041: スクロールバー **SHOULD**

- テーマに追従させる（FR-070）。`scrollbar-color: var(--fg-subtle) transparent` を指定する。
- 幅は OS の既定に従う。細くしすぎない。
- 本文・ペイン・ダイアログのいずれもスクロールバーの領域を確保し、スクロールの有無でレイアウトが横に揺れないよう `scrollbar-gutter: stable` を指定する。

## 20.6 モーション（DSP-050 系）

### DSP-050: アニメーションの方針 **MUST**

「動作が軽い」ことを優先し、装飾的なアニメーションを持たない（01-overview 1.3.2）。

| 対象 | 扱い |
| --- | --- |
| テーマ切り替え | トランジションなし（DSP-011） |
| ペインの開閉 | トランジションなし。即座に切り替える |
| ボタンのホバー | 背景色のみ 80 ms のフェード |
| コピー成功の表示 | アイコンの差し替えのみ。1.5 秒後に戻す |
| ダイアログの表示 | 100 ms のフェードイン |
| ドロップオーバーレイ | 80 ms のフェードイン |
| スクロール（アンカー移動） | 即座に移動する。スムーススクロールを使わない（FR-041） |
| 右クリックメニューの表示 | **トランジションなし。** 即座に出し、即座に消す（UI-085。押した直後に項目を選べるようにする） |
| 拡大画面の表示 | 100 ms のフェードイン（ダイアログと同じ）。閉じるときは即座に消す |
| 拡大画面の拡大・縮小・移動 | **トランジションなし。** ホイールやドラッグに遅れて追いつく動きを見せない（NFR-012） |
| 原寸表示の切り替え | トランジションなし |
| 編集モードの枠とラベル | トランジションなし。ボタンと同時に切り替わる |
| セルの確定・チェックボックスの反転 | トランジションなし（DSP-321） |

### DSP-051: 動きの抑制 **SHOULD**

`@media (prefers-reduced-motion: reduce)` の環境では、上記のフェードもすべて無効にする。

## 20.7 要求一覧

| ID | 概要 | 必須度 |
| --- | --- | --- |
| DSP-001 | 値の正 | MUST |
| DSP-002 | 実装との対応 | MUST |
| DSP-010 | 基本カラートークン | MUST |
| DSP-011 | テーマの適用機構 | MUST |
| DSP-012 | 本文の配色 | MUST |
| DSP-013 | シンタックスハイライト配色 | MUST |
| DSP-014 | アイコンの表示 | MUST |
| DSP-015 | 重なり順 | MUST |
| DSP-016 | フォーカス表示 | MUST |
| DSP-017 | アプリケーションアイコン | MUST |
| DSP-020 | 書体と字送り | MUST |
| DSP-021 | 表示倍率の適用範囲 | MUST |
| DSP-022 | 見出しの階層 | MUST |
| DSP-023 | 見出しのアンカーアイコン | SHOULD |
| DSP-030 | レイアウト寸法 | MUST |
| DSP-031 | 間隔の基準 | SHOULD |
| DSP-032 | ツールバー内の寸法 | MUST |
| DSP-040 | 角丸・境界・影 | MUST |
| DSP-041 | スクロールバー | SHOULD |
| DSP-050 | アニメーションの方針 | MUST |
| DSP-051 | 動きの抑制 | SHOULD |
